diff --git a/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js b/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js
index f09b6b4..c1168be 100644
--- a/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js
+++ b/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js
@@ -67,10 +67,29 @@ class DistributionIndexProcessor extends IndexProcessor_1.IndexProcessor {
         if (server == null) {
             throw new AssetGuardError_1.AssetGuardError(`Invalid server id ${this.serverId}`);
         }
-        const modLoaderModule = server.modules.find(({ rawModule: { type } }) => type === helios_distribution_types_1.Type.ForgeHosted || type === helios_distribution_types_1.Type.Forge || type === helios_distribution_types_1.Type.Fabric);
+        const modLoaderModule = server.modules.find(({ rawModule: { type } }) => type === helios_distribution_types_1.Type.ForgeHosted || type === helios_distribution_types_1.Type.Forge || type === helios_distribution_types_1.Type.Fabric || type === 'NeoForge');
         if (modLoaderModule == null) {
             throw new AssetGuardError_1.AssetGuardError('No mod loader found!');
         }
+        if (modLoaderModule.rawModule.type === 'NeoForge') {
+            const mavenComponents = modLoaderModule.getMavenComponents();
+            const versionId = `neoforge-${mavenComponents.version}`;
+            const versionJsonPath = (0, FileUtils_1.getVersionJsonPath)(this.commonDir, versionId);
+            if (await (0, fs_extra_1.pathExists)(versionJsonPath)) {
+                return await (0, fs_extra_1.readJson)(versionJsonPath, 'utf-8');
+            }
+            const zip = new node_stream_zip_1.default.async({ file: modLoaderModule.getPath() });
+            try {
+                const data = JSON.parse((await zip.entryData('version.json')).toString('utf8'));
+                const writePath = (0, FileUtils_1.getVersionJsonPath)(this.commonDir, data.id);
+                await (0, fs_extra_1.ensureDir)((0, path_1.dirname)(writePath));
+                await (0, fs_extra_1.writeJson)(writePath, data);
+                return data;
+            }
+            finally {
+                await zip.close();
+            }
+        }
         if (modLoaderModule.rawModule.type === helios_distribution_types_1.Type.Fabric
             || DistributionIndexProcessor.isForgeGradle3(server.rawServer.minecraftVersion, modLoaderModule.getMavenComponents().version)) {
             return await this.loadVersionManifest(modLoaderModule);
